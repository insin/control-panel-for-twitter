{
  "scripts": {
    "build": "web-ext build",
    "lint": "web-ext lint",
    "start": "web-ext run"
  },
  "webExt": {
    "ignoreFiles": [
      "icons/chrome-web-store-icon.png",
      "package.json",
      "README.md",
      "screenshots/"
    ]
  }
}
